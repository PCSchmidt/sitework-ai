# Demo clips

Pinned demo clips live here (git-ignored, `*.mp4`). The selected set and provenance:
**`data/manifests/pexels-demo-clips.yaml`** — 3 Pexels clips mapped 1:1 to the cameras in
`config/cameras.yaml` (dock / warehouse aisle / excavator yard) plus one PPE spare.
Preview frames in `_preview/` (also git-ignored).
