# Harness state

Sub-agent specs, memories, and refined policies persist here and are mounted into the
agent container at `/root/.prime` (docs/07 §1). Populated during M3 once the M3.0 spike
has validated the RPC surface; the spike's golden session becomes the CI contract-test
fixture.
