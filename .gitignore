# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Node / UI
node_modules/
ui/dist/

# Env / secrets
.env
*.local

# Data artifacts (large; tracked via manifests instead)
assets/clips/*.mp4
data/raw/
data/models/
/workspace/incidents/
/eval_workspace_tmp/
/golden_session_tmp/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
!.vscode/extensions.json

# Preview frames / thumbnails
assets/clips/_preview/

# Model weights (auto-downloaded)
*.pt
*.onnx
*.engine

# Spike-02 artifacts (training runs, fine-tune dataset)
runs/
data/raw/spike02-testset/
data/raw/spike02-finetune/

# Vendored prime-agent tarball (spike-01): private package, not on npm's public
# registry -- see docker/vendor/README.md. Never commit the binary itself.
docker/vendor/*.tgz
